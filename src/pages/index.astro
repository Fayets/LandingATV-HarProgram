---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import QuizForm from '../components/QuizForm.astro';

const mockups = [
  { src: '/images/mockup-1-test.png', alt: 'Test de sobrecarga: descubrí tu nivel en menos de 2 minutos.' },
  { src: '/images/mockup-2-descubri.png', alt: 'Descubrí cómo tu sobrecarga impacta tu vida hoy.' },
  { src: '/images/mockup-3-guia.png', alt: 'Recibí gratis tu guía para bajar tu sobrecarga.' },
];
---

<Layout>
  <Header />

  <main>
    <!-- TITULAR -->
    <h1>
      Descubrí gratis qué está aumentando tu sobrecarga<br />
      <span class="gold">y como reducirla... en menos de 60 segundos.</span>
    </h1>

    <!-- MOCKUP -->
    <div class="mockup-wrap">
      {mockups.map((m, i) => (
        <img
          class="imgMock"
          src={m.src}
          alt={m.alt}
          width="600"
          height="1158"
          loading={i === 0 ? 'eager' : 'lazy'}
          decoding="async"
        />
      ))}
    </div>

    <!-- FORMULARIO -->
    <QuizForm />

    <!-- BULLETS -->
    <ul class="bullets">
      <li><span class="dot"></span><span>Recibí tu resultado personalizado en menos de 60 segundos.</span></li>
      <li><span class="dot"></span><span>+500 personas ya identificaron la raiz de su sobrecarga.</span></li>
      <li><span class="dot"></span><span>100% gratuito — acceso instantáneo</span></li>
    </ul>

    <p class="fineprint">
      Al dejar tus datos aceptás que HAR se ponga en contacto con vos por teléfono, mensaje o email
      usando la información ingresada. No vendemos tu información personal. Al enviar el formulario
      aceptás nuestra Política de Privacidad y nuestros Términos de Servicio.
    </p>
  </main>

  <Footer />
</Layout>
